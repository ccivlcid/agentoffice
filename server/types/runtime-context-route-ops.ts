/**
 * RouteOpsExports — from registerRoutesPartC (routes/ops.ts).
 */

export interface RouteOpsExports {
  prettyStreamJson: (...args: any[]) => any;
  refreshCliUsageData: (...args: any[]) => any;
}
