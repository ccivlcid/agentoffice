# 문서고 (Agent Skills Library) 업무 프로세스

---

## 1. 개요

**문서고**는 사이드바 메뉴 `📚 문서고`로 접근하는 **AI 에이전트 스킬 관리 시스템**이다.
외부 레지스트리 [skills.sh](https://skills.sh)에서 스킬 목록을 실시간 동기화하고, 조직 내 AI 에이전트들에게 스킬을 학습시키거나 제거하는 전체 라이프사이클을 관리한다.

| 항목 | 값 |
|------|------|
| 사이드바 View 키 | `skills` |
| 아이콘 | 📚 |
| 메인 컴포넌트 | `src/components/SkillsLibrary.tsx` |
| 상태 관리 훅 | `src/components/skills-library/useSkillsLibrary.ts` |
| API 클라이언트 | `src/api/skills.ts` |
| 서버 라우트 | `server/modules/routes/ops/skills-list.ts`, `skills-learn.ts` |

---

## 2. 화면 구성

```
┌─────────────────────────────────────────────────┐
│  📚 Agent Skills 문서고          [등록 스킬 수]  │
│  AI 에이전트 스킬 디렉토리 · skills.sh 실시간 데이터│
├─────────────────────────────────────────────────┤
│  [ 스킬 검색 입력창 ... ]  [정렬: 순위/설치/이름]  │
├─────────────────────────────────────────────────┤
│  [전체] [프론트엔드] [백엔드] [디자인] [AI&Agent]  │
│  [마케팅] [테스트&QA] [데브옵스] [생산성] [아키텍처] │
│  [보안] [기타]                                    │
├─────────────────────────────────────────────────┤
│  "N개 스킬 표시중"                                │
├─────────────────────────────────────────────────┤
│  ┌─ 학습 메모리 ─────────────────────────┐       │
│  │  이력 탭 | 학습완료 탭               │       │
│  │  프로바이더 필터                      │       │
│  │  [학습 이력 테이블]                   │       │
│  └───────────────────────────────────────┘       │
├─────────────────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐                     │
│  │ Skill│ │ Skill│ │ Skill│  ← 스킬 카드 그리드  │
│  │ Card │ │ Card │ │ Card │    (1~3열 반응형)     │
│  └──────┘ └──────┘ └──────┘                     │
└─────────────────────────────────────────────────┘
```

### 2.1 주요 UI 영역

| 영역 | 설명 |
|------|------|
| **헤더** | 타이틀, 등록된 스킬 총 개수 표시 |
| **검색 & 정렬** | 이름/저장소/카테고리로 검색, 순위순·설치순·이름순 정렬 |
| **카테고리 필터** | 11개 카테고리 버튼 + 전체, 각 카테고리별 건수 표시 |
| **학습 메모리** | CLI별 스킬 학습 이력 패널 (`SkillHistoryPanel`) |
| **스킬 카드 그리드** | 개별 스킬 정보 카드 (호버 시 상세정보 로딩) |

---

## 3. 핵심 기능별 프로세스

### 3.1 스킬 탐색 (Discovery)

```
사용자 → 문서고 진입
         ↓
  GET /api/skills (서버)
         ↓
  서버: skills.sh HTML 파싱 → initialSkills JSON 추출
         ↓ (1시간 캐시)
  프론트: SkillEntry[] 수신 → categorize() 분류 → 카드 렌더
```

**데이터 흐름:**

1. `useSkillsLibrary` 훅이 마운트 시 `getSkills()` 호출
2. 서버(`skills-list.ts`)가 `https://skills.sh` HTML을 fetch → `initialSkills` JSON 배열 파싱
3. 1시간(3600초) 서버 메모리 캐시 적용
4. 프론트에서 `skillCategorize.ts`의 `categorize()` 함수로 11개 카테고리 자동 분류
5. 검색·정렬·카테고리 필터 적용 후 `SkillCard` 컴포넌트로 렌더링

**카테고리 분류 규칙** (`skillCategorize.ts`):

| 카테고리 | 아이콘 | 판별 키워드 예시 |
|----------|--------|----------------|
| Frontend | 🎨 | react, vue, next, tailwind, shadcn, vite |
| Backend | 🔧 | api, node, fastapi, python, sql, supabase, stripe |
| Design | ✨ | design, ui, ux, brand, theme, visual |
| AI & Agent | 🤖 | agent, mcp, prompt, langchain, rag, subagent |
| Marketing | 📈 | marketing, seo, copywriting, analytics, ads |
| Testing & QA | 🧪 | test, debug, audit, review, e2e |
| DevOps | 🚀 | docker, github-actions, deploy, monorepo, git |
| Productivity | 📝 | pdf, pptx, docx, slack, writing, obsidian |
| Architecture | 🏗️ | typescript, javascript, architecture, state-management |
| Security | 🔒 | security, accessibility |
| Other | 📦 | 위 분류에 해당하지 않는 나머지 |

### 3.2 스킬 상세 조회

```
스킬 카드 호버 (300ms 디바운스)
         ↓
  GET /api/skills/detail?source={repo}&skillId={id}
         ↓
  서버: skills.sh/{repo}/{skillId} 페이지 스크래핑
         ↓ (1시간 캐시, 최대 200건 LRU)
  프론트: 툴팁으로 상세정보 표시
```

**상세정보 (`SkillDetail`) 항목:**

| 필드 | 설명 |
|------|------|
| `title` | 스킬 제목 |
| `description` | 스킬 설명 |
| `whenToUse` | "When to Use This Skill" 항목 배열 |
| `weeklyInstalls` | 주간 설치 수 |
| `firstSeen` | 최초 등록일 |
| `installCommand` | 설치 명령어 (npx skills add ...) |
| `platforms` | 플랫폼별 설치 통계 (claude-code, codex, gemini-cli 등) |
| `audits` | 보안 감사 결과 (Gen Agent Trust Hub, Socket, Snyk) |

### 3.3 스킬 학습 (Learning)

```
스킬 카드 "학습" 버튼 클릭
         ↓
  SkillLearnModal 열림
         ↓
  프로바이더 선택 (claude, codex, gemini, opencode)
         ↓
  "학습 시작" 클릭
         ↓
  POST /api/skills/learn  →  Job 생성 (status: queued)
         ↓
  서버: npx skills@latest add {repo} --agent {agents} 실행
         ↓                              (status: running)
  프론트: 1.5초 간격 폴링 GET /api/skills/learn/{jobId}
         ↓
  실행 완료  →  status: succeeded / failed
         ↓
  학습 이력 DB 기록 (skill_learning_history 테이블)
         ↓
  히스토리 패널 자동 갱신
```

**학습 대상 프로바이더:**

| 프로바이더 | 에이전트 매핑 | 설명 |
|-----------|-------------|------|
| `claude` | Claude Code | Anthropic Claude 기반 에이전트 |
| `codex` | Codex CLI | OpenAI Codex 기반 에이전트 |
| `gemini` | Gemini CLI | Google Gemini 기반 에이전트 |
| `opencode` | OpenCode | OpenCode 기반 에이전트 |

**대표 에이전트 선정 로직:**
- 해당 프로바이더를 사용하는 에이전트 중 역할(팀장 > 시니어 > 주니어 > 인턴) → 경험치(XP) → ID 순으로 우선순위 결정

**Job 상태 머신:**
```
queued → running → succeeded
                 → failed
```

### 3.4 스킬 제거 (Unlearning)

```
SkillLearnModal → 학습 완료된 프로바이더 옆 "제거" 클릭
         ↓
  POST /api/skills/unlearn
  { provider, repo, skillId }
         ↓
  서버: npx skills remove 명령 실행
         ↓
  DB에서 해당 succeeded 이력 삭제
         ↓
  UI 제거 이펙트 재생 (pot/hammer 랜덤, 1.1초)
         ↓
  히스토리 갱신
```

### 3.5 학습 메모리 (History)

`SkillHistoryPanel` 컴포넌트가 두 탭으로 구성:

| 탭 | API | 설명 |
|----|-----|------|
| 이력 (history) | `GET /api/skills/history` | 전체 학습 시도 이력 (성공/실패 포함) |
| 학습완료 (available) | `GET /api/skills/available` | 현재 학습 완료된 스킬 목록 |

**데이터 보존 정책:**
- 보존 기간: `SKILL_LEARN_HISTORY_RETENTION_DAYS` (설정 가능)
- 프로바이더당 최대 행 수: `SKILL_LEARN_HISTORY_MAX_ROWS_PER_PROVIDER`
- 조회 시 자동 정리(pruning) 실행

---

## 4. API 명세

### 4.1 스킬 목록

```
GET /api/skills
→ { skills: SkillEntry[] }
```

### 4.2 스킬 상세

```
GET /api/skills/detail?source={repo}&skillId={id}
→ { ok: boolean, detail: SkillDetail | null }
```

### 4.3 스킬 학습 시작

```
POST /api/skills/learn
Body: { repo: string, skillId?: string, providers: SkillLearnProvider[] }
→ 202 { ok: true, job: SkillLearnJob }
```

### 4.4 학습 Job 상태 조회

```
GET /api/skills/learn/{jobId}
→ { ok: true, job: SkillLearnJob }
```

### 4.5 학습 이력 조회

```
GET /api/skills/history?provider={p}&status={s}&limit={n}
→ { ok: true, retention_days: number, history: SkillLearningHistoryEntry[] }
```

### 4.6 학습 완료 스킬 조회

```
GET /api/skills/available?provider={p}&limit={n}
→ { ok: true, skills: LearnedSkillEntry[] }
```

### 4.7 스킬 제거

```
POST /api/skills/unlearn
Body: { provider: string, repo: string, skillId?: string }
→ { ok: true, provider, repo, skill_id, removed: number }
```

---

## 5. 데이터 모델

### 5.1 SkillEntry (프론트)

```typescript
interface SkillEntry {
  rank: number;       // 순위
  name: string;       // 스킬 이름
  skillId: string;    // 스킬 고유 ID
  repo: string;       // GitHub 저장소 (owner/repo)
  installs: number;   // 총 설치 수
}
```

### 5.2 SkillLearnJob (학습 작업)

```typescript
interface SkillLearnJob {
  id: string;                        // UUID
  repo: string;                      // 대상 저장소
  skillId: string;                   // 스킬 ID
  providers: SkillLearnProvider[];   // 학습 대상 프로바이더
  agents: string[];                  // 매핑된 에이전트명
  status: SkillLearnStatus;          // queued | running | succeeded | failed
  command: string;                   // 실행된 npx 명령어
  createdAt: number;                 // 생성 시각 (ms)
  startedAt: number | null;          // 실행 시작 시각
  completedAt: number | null;        // 완료 시각
  updatedAt: number;                 // 최종 갱신 시각
  exitCode: number | null;           // 프로세스 종료 코드
  logTail: string[];                 // 최근 로그 라인
  error: string | null;              // 에러 메시지
}
```

### 5.3 skill_learning_history 테이블 (DB)

```sql
CREATE TABLE skill_learning_history (
  id              TEXT PRIMARY KEY,
  job_id          TEXT NOT NULL,
  provider        TEXT NOT NULL,
  repo            TEXT NOT NULL,
  skill_id        TEXT NOT NULL,
  skill_label     TEXT NOT NULL,
  status          TEXT NOT NULL,
  command         TEXT,
  error           TEXT,
  run_started_at  INTEGER,
  run_completed_at INTEGER,
  created_at      INTEGER NOT NULL,
  updated_at      INTEGER NOT NULL,
  UNIQUE(job_id, provider)
);
```

---

## 6. 파일 구조

```
src/
├── api/
│   └── skills.ts                          # API 클라이언트 함수
├── components/
│   ├── SkillsLibrary.tsx                  # 메인 뷰 (문서고 페이지)
│   ├── SkillHistoryPanel.tsx              # 학습 메모리 패널
│   └── skills-library/
│       ├── useSkillsLibrary.ts            # 상태 관리 커스텀 훅
│       ├── skillsLibraryHelpers.ts        # i18n, 카테고리, 포맷팅 유틸
│       ├── skillCategorize.ts             # 스킬 카테고리 자동 분류
│       ├── SkillCard.tsx                  # 스킬 카드 컴포넌트
│       ├── SkillLearnModal.tsx            # 학습 모달
│       ├── SkillLearnProviderCard.tsx     # 프로바이더 선택 카드
│       └── SkillsLibraryProviderLogos.tsx # 프로바이더 로고

server/
└── modules/routes/ops/
    ├── skills-list.ts                     # GET /api/skills, /api/skills/detail
    ├── skills-learn.ts                    # POST/GET 학습·이력·제거 라우트
    └── skills-learn-helpers.ts            # 상수, 유효성 검사, CLI 실행 유틸
```

---

## 7. 사용 가이드

### 7.1 스킬 검색 및 탐색

1. 사이드바에서 `📚 문서고` 클릭
2. 검색창에 키워드 입력 (이름, 저장소, 카테고리)
3. 카테고리 버튼으로 필터링
4. 정렬 드롭다운으로 순위순/설치순/이름순 변경
5. 스킬 카드에 마우스를 올리면 상세정보 툴팁 표시

### 7.2 에이전트에 스킬 학습시키기

1. 원하는 스킬 카드에서 **학습** 버튼 클릭
2. 학습 모달에서 대상 프로바이더 선택 (이미 학습된 프로바이더는 자동 제외)
3. **학습 시작** 클릭
4. 실시간 진행 상태 확인 (queued → running → succeeded/failed)
5. 완료 후 학습 메모리 패널에서 이력 확인

### 7.3 스킬 제거

1. 학습된 스킬의 카드에서 학습 버튼 클릭
2. 모달에서 학습 완료된 프로바이더 옆 **제거** 버튼 클릭
3. CLI 제거 명령 실행 및 DB 이력 삭제
4. 제거 이펙트 애니메이션 확인

### 7.4 학습 이력 확인

1. 문서고 화면 중앙의 **학습 메모리** 영역 확인
2. **이력** 탭: 전체 학습 시도 기록 (성공/실패 포함)
3. **학습완료** 탭: 현재 활성화된 학습 스킬 목록
4. 프로바이더 필터로 특정 CLI 기준 필터링

### 7.5 설치 명령어 복사

- 스킬 카드의 복사 버튼 클릭 → `npx skills add <owner/repo>` 클립보드 복사

---

## 8. 캐싱 및 성능

| 캐시 대상 | TTL | 저장 위치 | 비고 |
|-----------|-----|----------|------|
| 스킬 목록 | 1시간 | 서버 메모리 | 단일 객체 |
| 스킬 상세 | 1시간 | 서버 메모리 (Map) | 최대 200건 LRU, 초과 시 50건 정리 |
| 학습 이력 | 영구 | SQLite DB | retention 정책에 따라 자동 정리 |
| 학습 Job | 임시 | 서버 메모리 (Map) | TTL 초과 또는 최대 개수 초과 시 정리 |
| 상세 정보 | 세션 | 프론트 상태 (detailCache) | 호버 시 lazy load |

---

## 9. 다국어 지원

문서고는 4개 언어를 지원하며, `useI18n` 훅을 통해 자동 전환된다.

| 언어 | 코드 | 타이틀 |
|------|------|--------|
| 한국어 | `ko` | Agent Skills 문서고 |
| English | `en` | Agent Skills Library |
| 日本語 | `ja` | Agent Skills ライブラリ |
| 中文 | `zh` | Agent Skills 资料库 |

언어 설정은 `localStorage`의 `climpire.language` 키에 저장되며, 브라우저 언어 설정을 기본값으로 사용한다.

---

## 10. 에러 처리

| 상황 | 동작 |
|------|------|
| skills.sh 접속 불가 | "스킬 데이터를 불러올 수 없습니다" + 재시도 버튼 |
| 상세정보 로딩 실패 | 툴팁에 "error" 상태 표시 |
| 학습 Job 실패 | 모달에 에러 메시지 및 로그 표시 |
| 제거 CLI 실패 | `409 cli_unlearn_failed` 응답, 모달에 에러 표시 |
| 네트워크 타임아웃 | fetch 15초 AbortController 타임아웃 |

---

## 11. 커스텀 스킬 (로컬 등록)

`skills.sh` 레지스트리 외에, 사용자가 직접 스킬을 로컬 DB에 등록·관리할 수 있다.

### 11.1 개요

| 항목 | 값 |
|------|------|
| DB 테이블 | `custom_skills` |
| API 클라이언트 | `src/api/custom-skills.ts` |
| 상태 관리 훅 | `src/components/skills-library/useCustomSkills.ts` |
| 서버 라우트 | `server/modules/routes/ops/skill-documents.ts` |
| 생성/수정 모달 | `src/components/skills-library/SkillEditModal.tsx` |
| 삭제 확인 다이얼로그 | `src/components/skills-library/SkillDeleteConfirm.tsx` |

커스텀 스킬은 외부 레지스트리에 등록되지 않은 사내 전용 스킬, 실험 중인 스킬, 또는 오프라인 환경용 스킬을 관리할 때 활용된다.

### 11.2 커스텀 스킬 데이터 모델

```typescript
interface CustomSkill {
  id: string;           // UUID
  name: string;         // 스킬 이름 (필수)
  skill_id: string;     // 스킬 ID (미입력 시 이름과 동일)
  repo: string;         // GitHub 저장소 (owner/repo)
  category: string | null; // 카테고리
  description: string;  // 설명
  installs: number;     // 설치 수 (수동 입력)
  created_at: number;   // 생성 시각 (ms)
  updated_at: number;   // 수정 시각 (ms)
}
```

### 11.3 커스텀 스킬 CRUD 프로세스

**생성:**
```
[+ 스킬 등록] 버튼 클릭
         ↓
SkillEditModal (등록 모드) 열림
         ↓
이름(필수), 스킬 ID, 저장소, 카테고리, 설명, 설치 수 입력
         ↓
POST /api/custom-skills
         ↓
DB custom_skills 행 삽입
         ↓
카드 그리드에 즉시 반영
```

**수정:**
```
커스텀 스킬 카드 수정 아이콘 클릭
         ↓
SkillEditModal (수정 모드) 열림 — 기존 값 프리필
         ↓
PUT /api/custom-skills/:id
         ↓
DB 행 갱신, 카드 갱신
```

**삭제:**
```
커스텀 스킬 카드 삭제 아이콘 클릭
         ↓
SkillDeleteConfirm 다이얼로그 표시
         ↓
"삭제" 확인
         ↓
DELETE /api/custom-skills/:id
         ↓
카드 그리드에서 즉시 제거
```

### 11.4 커스텀 스킬 API 명세

```
GET    /api/custom-skills?search={q}&category={c}
→ { ok: true, skills: CustomSkill[] }

POST   /api/custom-skills
Body: { name, skill_id?, repo?, category?, description?, installs? }
→ { ok: true, id: string }

PUT    /api/custom-skills/:id
Body: { name?, skill_id?, repo?, category?, description?, installs? }
→ { ok: true }
※ 404 응답 시 not_found

DELETE /api/custom-skills/:id
→ { ok: true }
※ 404 응답 시 not_found
```

### 11.5 UI 통합

- `SkillsLibrary.tsx`에서 `useCustomSkills` 훅으로 관리
- `skills.sh` 기반 스킬과 커스텀 스킬이 **통합된 카드 그리드**에 함께 표시
- 커스텀 스킬 카드에는 수정/삭제 아이콘이 추가로 노출
- 전체 스킬 카운트(`totalCount`)에 커스텀 스킬 포함
- 카테고리 필터, 검색이 커스텀 스킬에도 동일 적용
