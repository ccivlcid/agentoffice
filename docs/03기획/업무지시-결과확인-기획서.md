# 업무지시 & 결과물 확인 & 테스트 메뉴 기획서

> 작성일: 2026-02-27 | 대상 버전: v1.3.0

---

## 0. 개요

### 목적
CEO(사용자)가 **업무지시를 직관적으로 내리고**, 완료된 **결과물을 체계적으로 확인**하며, 개발된 **소프트웨어를 테스트**할 수 있는 전용 메뉴를 추가한다.

### 현재 문제점
| 문제 | 설명 |
|------|------|
| 업무지시 경로 분산 | ChatPanel(`$` 디렉티브), TaskBoard(CreateModal), API 직접 호출 등 3개 경로가 분리되어 있음 |
| 결과물 확인 어려움 | 태스크 리포트가 모달(TaskReportPopup)에만 존재하고 체계적 열람 불가 |
| 진행 추적 단절 | 지시→실행→결과의 흐름을 한 화면에서 추적할 수 없음 |
| 테스트 수단 부재 | 완료된 개발 결과물을 즉시 테스트/미리보기할 방법이 없음 |

### 목표
1. **업무지시 센터** — 하나의 화면에서 업무를 지시하고 진행 상황을 추적
2. **결과물 열람** — 완료된 작업의 산출물(코드 diff, 문서, 리포트, 회의록)을 한 곳에서 확인
3. **테스트 센터** — 결과물에 대한 자동 테스트 실행, 미리보기, 수동 체크리스트 검증

### 전체 워크플로우

```
업무지시 (Directives)  →  실행 (Tasks)  →  결과물 (Deliverables)  →  테스트 (Testing)
    CEO가 지시            에이전트 작업       산출물 열람              품질 검증
       ↓                    ↓                  ↓                     ↓
   지시 생성/추적        자동 실행/모니터링    리포트/코드/문서        자동테스트/미리보기/체크리스트
```

---

## 1. 메뉴 구조 변경

### 1.1 Sidebar 네비게이션 추가

**AS-IS (5개 메뉴):**
```
Office | Skills | Dashboard | Tasks | Settings
```

**TO-BE (7개 메뉴):**
```
Office | Directives (NEW) | Dashboard | Tasks | Deliverables (NEW) | Skills | Settings
```

| 메뉴 | view 값 | 아이콘 | 설명 |
|------|---------|--------|------|
| **Directives** | `directives` | `Send` (lucide) | 업무지시 센터 |
| **Deliverables** | `deliverables` | `FolderCheck` (lucide) | 결과물 열람 |

### 1.2 로케일 텍스트

| 키 | ko | en | ja | zh |
|----|----|----|----|----|
| `directives` | 업무지시 | Directives | 業務指示 | 工作指示 |
| `deliverables` | 결과물 | Deliverables | 成果物 | 交付物 |

---

## 2. 업무지시 센터 (Directives View)

### 2.1 화면 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│  업무지시 센터                           [+ 새 업무지시]  │
├──────────────────────┬──────────────────────────────────┤
│                      │                                  │
│   지시 목록 (좌측)     │    지시 상세 / 새 지시 폼 (우측)    │
│                      │                                  │
│  ┌────────────────┐  │  ┌────────────────────────────┐  │
│  │ 📋 검색/필터    │  │  │  제목: [________________]   │  │
│  ├────────────────┤  │  │  내용: [________________]   │  │
│  │ #12 웹사이트 개편│  │  │        [________________]   │  │
│  │  개발팀 | 진행중  │  │  │  유형: [development ▼]     │  │
│  ├────────────────┤  │  │  부서: [자동배정 ▼]          │  │
│  │ #11 보고서 작성  │  │  │  프로젝트: [선택 ▼]         │  │
│  │  기획팀 | 완료   │  │  │  우선순위: ● High           │  │
│  ├────────────────┤  │  │                              │  │
│  │ #10 데이터 분석  │  │  │  [지시하기]  [임시저장]       │  │
│  │  분석팀 | 리뷰중  │  │  └────────────────────────────┘  │
│  └────────────────┘  │                                  │
│                      │  ── 또는 (선택 시) ──             │
│  필터 탭:            │                                  │
│  [전체|진행중|완료|보류]│  ┌────────────────────────────┐  │
│                      │  │ #12 웹사이트 개편             │  │
│                      │  │ 상태: in_progress             │  │
│                      │  │ 배정: 홍길동 (개발팀)          │  │
│                      │  │ 진행률: ████░░░░ 60%          │  │
│                      │  │                              │  │
│                      │  │ [터미널] [회의록] [중지]       │  │
│                      │  └────────────────────────────┘  │
└──────────────────────┴──────────────────────────────────┘
```

### 2.2 좌측 패널 — 지시 목록

| 구성 요소 | 설명 |
|-----------|------|
| **검색 바** | 제목/내용 키워드 검색 |
| **상태 필터 탭** | 전체 / 진행중(`inbox`+`planned`+`collaborating`+`in_progress`) / 완료(`done`) / 보류(`pending`+`review`+`cancelled`) |
| **부서 필터** | 드롭다운 — 전체 / 부서별 |
| **정렬** | 최신순(기본) / 우선순위순 / 상태순 |
| **지시 카드** | 번호, 제목, 배정부서, 상태 뱃지, 시간, 우선순위 도트 |

### 2.3 우측 패널 — 새 업무지시 폼

기존 `CreateModal`의 폼 필드를 인라인으로 재구성:

| 필드 | 필수 | 설명 |
|------|------|------|
| **제목** | O | 업무 지시 제목 |
| **상세 지시 내용** | O | 마크다운 지원 텍스트에어리어 |
| **업무 유형** | O | `development` / `design` / `analysis` / `presentation` / `documentation` / `general` |
| **대상 부서** | - | 미선택 시 자동배정 |
| **담당 에이전트** | - | 부서 내 에이전트 직접 지정 (선택) |
| **프로젝트** | - | 기존 프로젝트 선택 또는 새 프로젝트 생성 |
| **우선순위** | - | High / Medium / Low (기본: Medium) |
| **실행 모드** | - | Git Worktree / 로컬 직접 (development 유형 시만 표시) |

### 2.4 우측 패널 — 지시 상세 보기 (목록에서 선택 시)

| 섹션 | 내용 |
|------|------|
| **헤더** | 제목, 상태 뱃지, 우선순위, 생성일 |
| **지시 내용** | 마크다운 렌더링된 본문 |
| **배정 정보** | 부서명, 에이전트명, 프로젝트명 |
| **진행 상황** | 상태 타임라인 (inbox → planned → ... → done) |
| **서브태스크** | 하위 서브태스크 목록 및 상태 |
| **액션 버튼** | 상태에 따라 동적 표시 |

**상태별 액션 버튼:**

| 상태 | 사용 가능 액션 |
|------|---------------|
| `inbox` | 배정, 실행, 수정, 삭제 |
| `planned` | 실행, 수정, 취소 |
| `in_progress` | 터미널 보기, 일시정지, 중지 |
| `review` | 리뷰 결과 확인, 승인, 수정 요청 |
| `done` | 결과물 보기, 테스트 실행, 미리보기, 머지, 폐기 |
| `pending` / `cancelled` | 재실행, 삭제 |

### 2.5 빠른 지시 모드

ChatPanel의 `$` 디렉티브 기능을 업무지시 센터에서도 사용 가능:

```
┌────────────────────────────────────────┐
│ 💬 빠른 지시 입력                       │
│ ┌──────────────────────────────────┐   │
│ │ $ 홈페이지 메인 배너 디자인 수정해줘 │   │
│ └──────────────────────────────────┘   │
│  [전송] — 자동으로 프로젝트 연결 플로우  │
└────────────────────────────────────────┘
```

- 텍스트 입력 시 `$` 접두사 자동 추가
- Enter로 전송 → 프로젝트 선택 플로우 → 태스크 자동 생성
- 기존 `POST /api/directives` API 재사용

---

## 3. 결과물 열람 (Deliverables View)

### 3.1 화면 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│  결과물                       [프로젝트 ▼] [기간 ▼]      │
├──────────────────────┬──────────────────────────────────┤
│                      │                                  │
│   완료 목록 (좌측)     │    결과물 상세 (우측)              │
│                      │                                  │
│  ┌────────────────┐  │  ┌────────────────────────────┐  │
│  │ 🔍 검색        │  │  │  #11 보고서 작성             │  │
│  ├────────────────┤  │  │  완료: 2026-02-26 14:30     │  │
│  │ #11 보고서 작성 │  │  │  담당: 이영희 (기획팀)       │  │
│  │  기획팀 | 2/26  │  │  │                              │  │
│  ├────────────────┤  │  │  ┌──────────────────────┐    │  │
│  │ #9 API 리팩토링 │  │  │  │ 탭: [요약|코드|문서|회의록] │  │
│  │  개발팀 | 2/25  │  │  │  ├──────────────────────┤    │  │
│  ├────────────────┤  │  │  │                        │    │  │
│  │ #7 데이터 분석  │  │  │  │   요약 리포트 내용       │    │  │
│  │  분석팀 | 2/24  │  │  │  │   (마크다운 렌더링)      │    │  │
│  └────────────────┘  │  │  │                        │    │  │
│                      │  │  └──────────────────────────┘  │
│                      │  │                              │  │
│                      │  │  [아카이브] [다시 실행]        │  │
│                      │  └────────────────────────────┘  │
└──────────────────────┴──────────────────────────────────┘
```

### 3.2 좌측 패널 — 완료 태스크 목록

| 구성 요소 | 설명 |
|-----------|------|
| **검색 바** | 제목/내용 키워드 검색 |
| **프로젝트 필터** | 드롭다운 — 전체 / 프로젝트별 |
| **기간 필터** | 오늘 / 이번 주 / 이번 달 / 전체 |
| **유형 필터** | 전체 / development / design / analysis / ... |
| **결과물 카드** | 태스크 번호, 제목, 부서, 완료일, 유형 뱃지 |

데이터 소스: `GET /api/tasks?status=done` + `GET /api/task-reports`

### 3.3 우측 패널 — 결과물 상세

**탭 구성:**

| 탭 | 내용 | 데이터 소스 |
|----|------|-------------|
| **요약** | 태스크 리포트 마크다운 (summary_markdown) | `GET /api/task-reports/:taskId` |
| **코드** | Git diff 뷰 (development 유형만) | `GET /api/tasks/:id/diff` |
| **테스트** | 자동 테스트 실행 + 미리보기 + 체크리스트 | `POST /api/tasks/:id/test`, `POST /api/tasks/:id/preview` |
| **문서** | 생성된 문서/산출물 목록 | `TaskReportDocument[]` |
| **회의록** | 기획/리뷰 회의 기록 | `GET /api/tasks/:id/meeting-minutes` |
| **실행 로그** | CLI 출력 기록 | `GET /api/tasks/:id/terminal` |

### 3.4 요약 탭 상세

```
┌────────────────────────────────────────┐
│ 📋 태스크 요약                          │
│                                        │
│ 상태: ✅ 완료                           │
│ 기간: 2026-02-25 10:00 → 02-26 14:30   │
│ 소요 시간: 1일 4시간 30분                │
│                                        │
│ ── 실행 요약 ──                         │
│ (summary_markdown 렌더링)              │
│                                        │
│ ── 기획 요약 ──                         │
│ (planning_summary 렌더링)              │
│                                        │
│ ── 팀 리포트 ──                         │
│ 개발팀: 홍길동 — 메인 로직 구현          │
│ QA팀: 김철수 — 테스트 코드 작성          │
│                                        │
│ ── 서브태스크 ──                        │
│ [✅] 프론트엔드 컴포넌트 개발 (개발팀)    │
│ [✅] API 엔드포인트 추가 (개발팀)        │
│ [✅] 유닛 테스트 작성 (QA팀)             │
└────────────────────────────────────────┘
```

### 3.5 코드 탭 상세 (development 유형)

기존 `DiffModal` 컴포넌트를 인라인으로 재활용:
- 파일별 변경사항 (추가/수정/삭제)
- 라인 하이라이팅
- 파일 트리 네비게이션
- **머지 / 폐기** 버튼 (아직 머지 안 된 경우)

### 3.6 액션 버튼

| 액션 | 조건 | API |
|------|------|-----|
| **아카이브** | done 상태 | `POST /api/task-reports/:taskId/archive` |
| **다시 실행** | done 상태 | 새 태스크 생성 (같은 설정 복제) |
| **테스트 실행** | done + development | `POST /api/tasks/:id/test` |
| **미리보기** | done + development (웹) | `POST /api/tasks/:id/preview` |
| **머지** | development + 미머지 | `POST /api/tasks/:id/merge` |
| **폐기** | development + 미머지 | `POST /api/tasks/:id/discard` |

### 3.7 테스트 탭 상세

결과물 상세의 "테스트" 탭은 3개 하위 섹션으로 구성:

```
┌────────────────────────────────────────────────────────┐
│  테스트                                                 │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 섹션: [자동 테스트 | 미리보기 | 체크리스트]         │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │                                                  │  │
│  │  (선택된 섹션 내용)                                │  │
│  │                                                  │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

---

## 4. 테스트 센터 (Deliverables 테스트 탭)

### 4.1 자동 테스트 실행

프로젝트에 설정된 테스트 스크립트를 자동 감지하고 실행.

#### 4.1.1 테스트 스크립트 자동 감지

프로젝트 경로에서 다음 파일을 탐색하여 테스트 명령어를 자동 감지:

| 감지 대상 | 테스트 명령어 | 비고 |
|-----------|-------------|------|
| `package.json` → `scripts.test` | `npm test` / `pnpm test` | Node.js 프로젝트 |
| `package.json` → `scripts.test:unit` | 해당 스크립트 실행 | 유닛 테스트 분리 시 |
| `package.json` → `scripts.test:e2e` | 해당 스크립트 실행 | E2E 테스트 분리 시 |
| `pytest.ini` / `pyproject.toml` | `pytest` | Python 프로젝트 |
| `Cargo.toml` | `cargo test` | Rust 프로젝트 |
| `go.mod` | `go test ./...` | Go 프로젝트 |
| `Makefile` → `test` 타겟 | `make test` | 범용 |

#### 4.1.2 화면 레이아웃

```
┌────────────────────────────────────────────────────────┐
│  자동 테스트                                             │
│                                                        │
│  감지된 테스트:                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │ npm test        (package.json → scripts.test)    │  │
│  │ npm run test:e2e (package.json → scripts.test:e2e)│  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  [전체 실행]  [선택 실행 ▼]                               │
│                                                        │
│  ── 실행 결과 ──                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │ npm test                                         │  │
│  │ 상태: PASS                                       │  │
│  │ 실행 시간: 12.3s                                  │  │
│  │ 결과: 45 passed, 0 failed, 2 skipped             │  │
│  │                                                  │  │
│  │  Tests:  45 passed                               │  │
│  │  Suites: 8 passed                                │  │
│  │  Time:   12.345s                                 │  │
│  │                                                  │  │
│  │ [상세 로그 보기 ▼]                                │  │
│  │ ┌────────────────────────────────────────────┐   │  │
│  │ │ PASS src/utils/math.test.ts (0.5s)        │   │  │
│  │ │ PASS src/api/tasks.test.ts (1.2s)         │   │  │
│  │ │ PASS src/components/Button.test.tsx (0.8s) │   │  │
│  │ │ ...                                       │   │  │
│  │ └────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  ── 실행 이력 ──                                        │
│  │ 2026-02-27 14:30  PASS  45/45  12.3s              │  │
│  │ 2026-02-27 10:15  FAIL  43/45  11.8s              │  │
│  │ 2026-02-26 16:00  PASS  42/42  10.2s              │  │
└────────────────────────────────────────────────────────┘
```

#### 4.1.3 테스트 결과 파싱

CLI 출력에서 테스트 결과를 구조화하여 표시:

| 파싱 대상 | 추출 정보 |
|-----------|-----------|
| Jest/Vitest | Tests passed/failed/skipped, Suites, Time |
| pytest | passed/failed/error/skipped, Duration |
| go test | ok/FAIL, 패키지별 결과 |
| cargo test | test result: ok/FAILED, 개수 |

실패한 테스트는 **빨간색 하이라이팅** + 에러 메시지 표시.

#### 4.1.4 실행 옵션

| 옵션 | 설명 |
|------|------|
| **워크트리 경로에서 실행** | 미머지 상태의 Git Worktree 경로에서 테스트 (기본) |
| **프로젝트 경로에서 실행** | 머지 완료 후 메인 프로젝트에서 테스트 |
| **커스텀 명령어** | 사용자가 직접 테스트 명령어 입력 |
| **환경 변수** | 테스트 실행 시 추가 환경 변수 설정 |

### 4.2 미리보기 (Preview)

개발 서버를 실행하고 결과물을 브라우저에서 미리 확인.

#### 4.2.1 개발 서버 자동 감지

| 감지 대상 | 시작 명령어 | 기본 포트 |
|-----------|------------|----------|
| `package.json` → `scripts.dev` | `npm run dev` | 3000/5173 |
| `package.json` → `scripts.start` | `npm start` | 3000 |
| `package.json` → `scripts.preview` | `npm run preview` | 4173 |
| `manage.py` | `python manage.py runserver` | 8000 |
| `main.py` / `app.py` | `python main.py` | 8000 |

#### 4.2.2 화면 레이아웃

```
┌────────────────────────────────────────────────────────┐
│  미리보기                                               │
│                                                        │
│  서버 상태: ● 실행 중 (http://localhost:5173)            │
│  명령어: npm run dev                                    │
│  실행 시간: 2분 30초                                    │
│                                                        │
│  [브라우저에서 열기]  [서버 중지]  [재시작]                 │
│                                                        │
│  ── 서버 로그 ──                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │ VITE v6.1.0  ready in 1234 ms                    │  │
│  │                                                  │  │
│  │   ➜  Local:   http://localhost:5173/             │  │
│  │   ➜  Network: http://192.168.1.100:5173/        │  │
│  │                                                  │  │
│  │ [HMR] connected                                  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  ── 또는 iframe 프리뷰 (선택 설정) ──                    │
│  ┌──────────────────────────────────────────────────┐  │
│  │                                                  │  │
│  │          (iframe: localhost:5173)                 │  │
│  │                                                  │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

#### 4.2.3 미리보기 동작 방식

1. **서버 시작**: 감지된 명령어로 프로세스 스폰 (기존 CLI 프로세스 매니저 재활용)
2. **포트 대기**: 서버가 포트를 열 때까지 폴링 (최대 30초)
3. **접속 방법**:
   - **브라우저 열기**: 기본 브라우저에서 `http://localhost:{port}` 오픈 (Electron의 `shell.openExternal`)
   - **인라인 프리뷰**: iframe으로 앱 내 임베드 (선택적, 설정으로 토글)
4. **서버 관리**: 프리뷰 탭 이탈 또는 수동 중지 시 프로세스 종료
5. **포트 충돌**: 이미 사용 중인 포트 감지 시 다른 포트 자동 할당 (`--port` 플래그)

#### 4.2.4 Electron 환경 특수 처리

| 상황 | 처리 |
|------|------|
| **Electron 패키지 모드** | `shell.openExternal(url)` 으로 외부 브라우저 열기 |
| **개발 모드** | 동일 브라우저 새 탭 (iframe 또는 `window.open`) |
| **포트 점유 충돌** | 동적 포트 할당 후 URL 표시 |

### 4.3 테스트 체크리스트

AI가 태스크 내용을 기반으로 수동 테스트 항목을 자동 생성하고, CEO가 하나씩 체크.

#### 4.3.1 화면 레이아웃

```
┌────────────────────────────────────────────────────────┐
│  테스트 체크리스트                                       │
│                                                        │
│  진행률: ████████░░ 80% (8/10 항목 완료)                │
│                                                        │
│  [체크리스트 재생성]  [항목 추가]                          │
│                                                        │
│  ── 기능 테스트 ──                                      │
│  [x] 로그인 페이지가 정상적으로 표시되는지 확인            │
│  [x] 이메일/비밀번호 입력 후 로그인 성공 확인              │
│  [x] 잘못된 비밀번호 입력 시 에러 메시지 표시 확인         │
│  [ ] 비밀번호 찾기 링크가 동작하는지 확인                  │
│  [x] 로그인 후 대시보드로 리디렉션 확인                   │
│                                                        │
│  ── UI/UX 테스트 ──                                     │
│  [x] 모바일 반응형 레이아웃 확인 (375px)                  │
│  [x] 다크 모드에서 정상 표시 확인                         │
│  [x] 로딩 스피너 표시 확인                               │
│                                                        │
│  ── 엣지 케이스 ──                                      │
│  [ ] 빈 입력 제출 시 유효성 검사 메시지 확인               │
│  [x] 네트워크 에러 시 에러 처리 확인                      │
│                                                        │
│  ── 메모 ──                                             │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 비밀번호 찾기 링크 404 오류 발생 — 수정 필요        │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  [테스트 완료]  [수정 요청]                               │
└────────────────────────────────────────────────────────┘
```

#### 4.3.2 체크리스트 자동 생성 방식

AI 에이전트가 다음 컨텍스트를 기반으로 체크리스트 생성:

| 입력 | 활용 |
|------|------|
| **태스크 제목/설명** | 핵심 기능 테스트 항목 도출 |
| **코드 diff** | 변경된 파일/함수 기반 테스트 포인트 도출 |
| **태스크 유형** | 유형별 기본 체크 항목 (UI→반응형, API→엔드포인트, 등) |
| **회의록** | 리뷰에서 지적된 사항 반영 |

#### 4.3.3 체크리스트 아이템 구조

```typescript
interface ChecklistItem {
  id: string;
  category: 'functional' | 'ui_ux' | 'edge_case' | 'performance' | 'security';
  text: string;
  checked: boolean;
  checkedAt?: string;     // ISO timestamp
  note?: string;          // CEO 메모
  autoGenerated: boolean; // AI 자동 생성 여부
}
```

#### 4.3.4 체크리스트 워크플로우

```
태스크 완료 (done 상태)
  ↓
체크리스트 자동 생성 요청 (POST /api/tasks/:id/checklist/generate)
  ↓
AI 에이전트가 컨텍스트 분석 → 항목 생성
  ↓
CEO가 항목별 체크/메모
  ↓
전체 통과 → [테스트 완료] 클릭
  ↓
실패 항목 존재 → [수정 요청] 클릭 → 실패 항목 기반 새 태스크 생성
```

#### 4.3.5 수동 항목 추가

CEO가 직접 테스트 항목을 추가할 수 있음:
- "항목 추가" 버튼 클릭 → 카테고리 선택 + 텍스트 입력
- `autoGenerated: false` 로 구분

### 4.4 테스트 결과 종합 뱃지

Deliverables 목록 카드에 테스트 상태 뱃지 표시:

| 뱃지 | 조건 | 색상 |
|------|------|------|
| **미실행** | 테스트 미수행 | 회색 |
| **통과** | 자동 테스트 PASS + 체크리스트 100% | 초록 |
| **부분 통과** | 일부 통과 | 노란 |
| **실패** | 자동 테스트 FAIL 또는 체크리스트 미완 | 빨간 |

---

## 5. 백엔드 변경사항

### 5.1 기존 API 활용 (신규 엔드포인트 최소화)

대부분의 데이터는 기존 API로 충분:

| 기능 | 기존 API |
|------|----------|
| 업무지시 생성 | `POST /api/tasks` |
| 빠른 지시 | `POST /api/directives` |
| 지시 목록 조회 | `GET /api/tasks` (필터 활용) |
| 지시 상세 조회 | `GET /api/tasks/:id` |
| 결과물 목록 | `GET /api/task-reports` |
| 결과물 상세 | `GET /api/task-reports/:taskId` |
| 코드 diff | `GET /api/tasks/:id/diff` |
| 회의록 | `GET /api/tasks/:id/meeting-minutes` |
| 실행 로그 | `GET /api/tasks/:id/terminal` |

### 5.2 신규/확장 API

| 메서드 | 경로 | 설명 |
|--------|------|------|
| `GET` | `/api/tasks/:id/timeline` | 태스크 상태 변경 이력 (타임라인용) |
| `POST` | `/api/tasks/:id/clone` | 태스크 복제 (다시 실행용) |
| `POST` | `/api/tasks/:id/test` | 자동 테스트 실행 |
| `GET` | `/api/tasks/:id/test` | 테스트 결과/상태 조회 |
| `GET` | `/api/tasks/:id/test/detect` | 프로젝트 테스트 스크립트 자동 감지 |
| `POST` | `/api/tasks/:id/preview` | 미리보기 서버 시작 |
| `DELETE` | `/api/tasks/:id/preview` | 미리보기 서버 중지 |
| `GET` | `/api/tasks/:id/preview` | 미리보기 서버 상태 조회 |
| `POST` | `/api/tasks/:id/checklist/generate` | AI 체크리스트 자동 생성 |
| `GET` | `/api/tasks/:id/checklist` | 체크리스트 조회 |
| `PATCH` | `/api/tasks/:id/checklist/:itemId` | 체크리스트 항목 업데이트 |
| `POST` | `/api/tasks/:id/checklist` | 체크리스트 수동 항목 추가 |
| `DELETE` | `/api/tasks/:id/checklist/:itemId` | 체크리스트 항목 삭제 |

### 5.3 태스크 타임라인 API 상세

```
GET /api/tasks/:id/timeline

Response:
{
  "timeline": [
    { "status": "inbox",       "at": "2026-02-25T10:00:00Z", "by": "CEO" },
    { "status": "planned",     "at": "2026-02-25T10:05:00Z", "by": "기획팀장" },
    { "status": "in_progress", "at": "2026-02-25T10:30:00Z", "by": "홍길동" },
    { "status": "review",      "at": "2026-02-26T14:00:00Z", "by": "system" },
    { "status": "done",        "at": "2026-02-26T14:30:00Z", "by": "CEO" }
  ]
}
```

데이터 소스: 기존 `task_logs` 테이블 활용

### 5.4 태스크 복제 API 상세

```
POST /api/tasks/:id/clone

Response:
{ "id": 13, "title": "[재실행] 보고서 작성", "status": "inbox" }
```

원본 태스크의 `title`, `description`, `task_type`, `department_id`, `project_id`, `priority`를 복사하여 새 `inbox` 태스크 생성.

### 5.5 테스트 관련 API 상세

#### 테스트 스크립트 감지

```
GET /api/tasks/:id/test/detect

Response:
{
  "scripts": [
    { "name": "npm test",       "source": "package.json → scripts.test",     "type": "unit" },
    { "name": "npm run test:e2e","source": "package.json → scripts.test:e2e", "type": "e2e" }
  ],
  "projectPath": "/home/user/project",
  "worktreePath": "/home/user/.worktrees/task-12"
}
```

#### 테스트 실행

```
POST /api/tasks/:id/test

Body:
{
  "command": "npm test",          // 선택 — 미지정 시 감지된 기본 명령어
  "cwd": "worktree",              // "worktree" | "project" — 실행 경로
  "env": { "CI": "true" }         // 추가 환경 변수 (선택)
}

Response:
{
  "testRunId": "tr_abc123",
  "status": "running"
}
```

테스트 출력은 WebSocket `test_output` 이벤트로 실시간 스트리밍.

#### 테스트 결과 조회

```
GET /api/tasks/:id/test

Response:
{
  "runs": [
    {
      "id": "tr_abc123",
      "command": "npm test",
      "status": "passed",            // "running" | "passed" | "failed" | "error"
      "startedAt": "2026-02-27T14:30:00Z",
      "finishedAt": "2026-02-27T14:30:12Z",
      "duration": 12345,             // ms
      "summary": {
        "total": 45,
        "passed": 45,
        "failed": 0,
        "skipped": 2
      },
      "output": "...(CLI 출력 원문, 최대 50KB)..."
    }
  ]
}
```

#### 미리보기 서버 시작

```
POST /api/tasks/:id/preview

Body:
{
  "command": "npm run dev",       // 선택 — 미지정 시 자동 감지
  "port": 5173                    // 선택 — 미지정 시 자동 할당
}

Response:
{
  "previewId": "pv_xyz789",
  "pid": 12345,
  "url": "http://localhost:5173",
  "status": "starting"
}
```

서버 준비 완료 시 WebSocket `preview_ready` 이벤트 전송.

#### 미리보기 서버 상태

```
GET /api/tasks/:id/preview

Response:
{
  "previewId": "pv_xyz789",
  "pid": 12345,
  "url": "http://localhost:5173",
  "status": "running",              // "starting" | "running" | "stopped" | "error"
  "startedAt": "2026-02-27T14:35:00Z",
  "uptime": 150                     // seconds
}
```

#### 체크리스트 자동 생성

```
POST /api/tasks/:id/checklist/generate

Response:
{
  "items": [
    {
      "id": "cl_001",
      "category": "functional",
      "text": "로그인 페이지가 정상적으로 표시되는지 확인",
      "checked": false,
      "autoGenerated": true
    },
    ...
  ]
}
```

AI 에이전트 (oneshot 모드)를 사용하여 태스크 컨텍스트(제목, 설명, diff, 회의록)를 분석하고 체크리스트 생성.

### 5.6 DB 스키마 확장

#### test_runs 테이블

```sql
CREATE TABLE test_runs (
  id TEXT PRIMARY KEY,
  task_id INTEGER NOT NULL REFERENCES tasks(id),
  command TEXT NOT NULL,
  cwd TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'running',  -- running, passed, failed, error
  started_at TEXT NOT NULL,
  finished_at TEXT,
  duration INTEGER,                        -- ms
  total INTEGER DEFAULT 0,
  passed INTEGER DEFAULT 0,
  failed INTEGER DEFAULT 0,
  skipped INTEGER DEFAULT 0,
  output TEXT,                             -- CLI 출력 (최대 50KB)
  created_at TEXT DEFAULT (datetime('now'))
);
```

#### preview_sessions 테이블

```sql
CREATE TABLE preview_sessions (
  id TEXT PRIMARY KEY,
  task_id INTEGER NOT NULL REFERENCES tasks(id),
  command TEXT NOT NULL,
  pid INTEGER,
  port INTEGER NOT NULL,
  url TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'starting',  -- starting, running, stopped, error
  started_at TEXT NOT NULL,
  stopped_at TEXT,
  created_at TEXT DEFAULT (datetime('now'))
);
```

#### test_checklists 테이블

```sql
CREATE TABLE test_checklists (
  id TEXT PRIMARY KEY,
  task_id INTEGER NOT NULL REFERENCES tasks(id),
  category TEXT NOT NULL,                   -- functional, ui_ux, edge_case, performance, security
  text TEXT NOT NULL,
  checked INTEGER NOT NULL DEFAULT 0,
  checked_at TEXT,
  note TEXT,
  auto_generated INTEGER NOT NULL DEFAULT 1,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TEXT DEFAULT (datetime('now'))
);
```

### 5.7 WebSocket 이벤트 추가

| 이벤트 | 배칭 | 설명 |
|--------|------|------|
| `test_output` | 250ms | 테스트 실행 CLI 출력 스트리밍 |
| `test_complete` | 즉시 | 테스트 실행 완료 (결과 요약 포함) |
| `preview_ready` | 즉시 | 미리보기 서버 준비 완료 (URL 포함) |
| `preview_stopped` | 즉시 | 미리보기 서버 종료 |

---

## 6. 프론트엔드 구현

### 6.1 신규 파일

| 파일 | 설명 |
|------|------|
| `src/components/directives/DirectivesView.tsx` | 업무지시 센터 메인 뷰 |
| `src/components/directives/DirectivesList.tsx` | 좌측 지시 목록 패널 |
| `src/components/directives/DirectiveCard.tsx` | 개별 지시 카드 |
| `src/components/directives/DirectiveForm.tsx` | 새 업무지시 폼 |
| `src/components/directives/DirectiveDetail.tsx` | 지시 상세 보기 |
| `src/components/directives/DirectiveTimeline.tsx` | 상태 타임라인 위젯 |
| `src/components/directives/QuickDirectiveBar.tsx` | 빠른 지시 입력 바 |
| `src/components/deliverables/DeliverablesView.tsx` | 결과물 메인 뷰 |
| `src/components/deliverables/DeliverablesList.tsx` | 좌측 완료 목록 패널 |
| `src/components/deliverables/DeliverableCard.tsx` | 결과물 카드 |
| `src/components/deliverables/DeliverableDetail.tsx` | 결과물 상세 (탭) |
| `src/components/deliverables/DeliverableSummaryTab.tsx` | 요약 탭 |
| `src/components/deliverables/DeliverableCodeTab.tsx` | 코드 diff 탭 |
| `src/components/deliverables/DeliverableTestTab.tsx` | 테스트 탭 (메인 컨테이너) |
| `src/components/deliverables/DeliverableMinutesTab.tsx` | 회의록 탭 |
| `src/components/deliverables/DeliverableLogTab.tsx` | 실행 로그 탭 |
| `src/components/deliverables/testing/AutoTestSection.tsx` | 자동 테스트 실행 섹션 |
| `src/components/deliverables/testing/PreviewSection.tsx` | 미리보기 서버 관리 섹션 |
| `src/components/deliverables/testing/ChecklistSection.tsx` | 테스트 체크리스트 섹션 |
| `src/components/deliverables/testing/TestResultCard.tsx` | 개별 테스트 결과 카드 |
| `src/components/deliverables/testing/ChecklistItem.tsx` | 체크리스트 아이템 |

### 6.2 수정 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/App.tsx` | `View` 타입에 `"directives"` `"deliverables"` 추가, 뷰 렌더링 분기 추가 |
| `src/components/Sidebar.tsx` | `NAV_ITEMS`에 2개 메뉴 추가 |
| `src/components/AppHeader.tsx` | (선택) 헤더에 빠른 지시 버튼 추가 |
| `src/hooks/useAppLabels.ts` | 로케일 텍스트 추가 |
| `src/api/tasks.ts` | `getTaskTimeline()`, `cloneTask()` 함수 추가 |
| `src/api/testing.ts` | **(신규)** 테스트/미리보기/체크리스트 API 클라이언트 |
| `src/hooks/useAppWebSocket.ts` | `test_output`, `test_complete`, `preview_ready`, `preview_stopped` 이벤트 핸들러 추가 |

### 6.3 재활용 컴포넌트

| 기존 컴포넌트 | 재활용 위치 |
|--------------|-------------|
| `CreateModalForm` | DirectiveForm 내부 (폼 필드 추출) |
| `CreateModalOverlays` | DirectiveForm 프로젝트 선택 |
| `DiffModal` (내부 뷰어) | DeliverableCodeTab |
| `TerminalPanel` | DeliverableLogTab, AutoTestSection (CLI 출력 표시) |
| `TerminalMinutesTab` | DeliverableMinutesTab |
| `TaskCardActions` | DirectiveDetail 액션 버튼 |
| `ExecutionModeDialog` | DirectiveForm 실행 모드 선택 |
| `ProjectFlowModal` | QuickDirectiveBar 프로젝트 연결 |

### 6.4 View 타입 변경

```typescript
// AS-IS
type View = "office" | "dashboard" | "tasks" | "skills" | "settings";

// TO-BE
type View = "office" | "directives" | "dashboard" | "tasks" | "deliverables" | "skills" | "settings";
```

### 6.5 신규 API 클라이언트 (`src/api/testing.ts`)

```typescript
// 자동 테스트
export function detectTestScripts(taskId: number): Promise<DetectedScripts>;
export function runTest(taskId: number, opts?: RunTestOpts): Promise<TestRunStatus>;
export function getTestResults(taskId: number): Promise<TestRunResult[]>;

// 미리보기
export function startPreview(taskId: number, opts?: PreviewOpts): Promise<PreviewSession>;
export function getPreviewStatus(taskId: number): Promise<PreviewSession | null>;
export function stopPreview(taskId: number): Promise<void>;

// 체크리스트
export function generateChecklist(taskId: number): Promise<ChecklistItem[]>;
export function getChecklist(taskId: number): Promise<ChecklistItem[]>;
export function updateChecklistItem(taskId: number, itemId: string, data: Partial<ChecklistItem>): Promise<void>;
export function addChecklistItem(taskId: number, item: NewChecklistItem): Promise<ChecklistItem>;
export function deleteChecklistItem(taskId: number, itemId: string): Promise<void>;
```

---

## 7. 데이터 흐름

### 7.1 업무지시 생성 플로우

```
[DirectiveForm] 작성 완료
  ↓
POST /api/tasks (title, description, task_type, department_id, project_id, priority)
  ↓
WebSocket: task_update 이벤트
  ↓
[DirectivesList] 실시간 반영
  ↓
(자동배정 또는 수동배정)
  ↓
POST /api/tasks/:id/run
  ↓
WebSocket: cli_output 스트리밍
  ↓
[DirectiveDetail] 진행 상황 실시간 표시
```

### 7.2 빠른 지시 플로우

```
[QuickDirectiveBar] "$ 지시 내용" 입력
  ↓
POST /api/directives (content, project_id)
  ↓
서버: 태스크 자동 생성 + 부서 위임
  ↓
WebSocket: task_update
  ↓
[DirectivesList] 실시간 반영
```

### 7.3 결과물 조회 플로우

```
[DeliverablesView] 마운트
  ↓
GET /api/task-reports (완료된 리포트 목록)
  ↓
[DeliverablesList] 렌더링
  ↓
사용자가 항목 클릭
  ↓
GET /api/task-reports/:taskId (상세 리포트)
  ↓
[DeliverableDetail] 탭 렌더링
  ↓
탭 전환 시 lazy loading:
  - 코드 탭: GET /api/tasks/:id/diff
  - 테스트 탭: GET /api/tasks/:id/test/detect + GET /api/tasks/:id/test + GET /api/tasks/:id/checklist
  - 회의록 탭: GET /api/tasks/:id/meeting-minutes
  - 로그 탭: GET /api/tasks/:id/terminal
```

### 7.4 테스트 실행 플로우

```
[DeliverableTestTab] 테스트 탭 진입
  ↓
GET /api/tasks/:id/test/detect (테스트 스크립트 감지)
  ↓
[AutoTestSection] 감지된 스크립트 표시
  ↓
사용자가 "전체 실행" 클릭
  ↓
POST /api/tasks/:id/test (테스트 실행 요청)
  ↓
WebSocket: test_output (실시간 CLI 출력)
  ↓
[AutoTestSection] 출력 스트리밍 표시
  ↓
WebSocket: test_complete (테스트 완료 + 결과 요약)
  ↓
[TestResultCard] 결과 카드 렌더링 (passed/failed/skipped)
```

### 7.5 미리보기 플로우

```
[PreviewSection] 미리보기 섹션 진입
  ↓
GET /api/tasks/:id/preview (기존 세션 확인)
  ↓
사용자가 "서버 시작" 클릭
  ↓
POST /api/tasks/:id/preview (개발 서버 스폰)
  ↓
WebSocket: preview_ready (서버 준비 완료, URL 포함)
  ↓
[PreviewSection] URL 표시 + "브라우저에서 열기" 버튼
  ↓
사용자가 브라우저 열기 클릭
  ↓
Electron: shell.openExternal(url) / 웹: window.open(url)
  ↓
테스트 완료 후 "서버 중지" 클릭
  ↓
DELETE /api/tasks/:id/preview
```

### 7.6 체크리스트 플로우

```
[ChecklistSection] 체크리스트 섹션 진입
  ↓
GET /api/tasks/:id/checklist (기존 체크리스트 조회)
  ↓
체크리스트 없음 → "체크리스트 생성" 버튼 표시
  ↓
사용자가 "체크리스트 생성" 클릭
  ↓
POST /api/tasks/:id/checklist/generate (AI 생성)
  ↓
[ChecklistSection] 생성된 항목 렌더링
  ↓
사용자가 항목 체크/메모 입력
  ↓
PATCH /api/tasks/:id/checklist/:itemId (항목 업데이트)
  ↓
전체 통과 → [테스트 완료] 또는 실패 → [수정 요청] → POST /api/tasks/:id/clone
```

---

## 8. UI/UX 세부 사항

### 8.1 반응형 디자인

| 화면 크기 | 레이아웃 |
|-----------|----------|
| Desktop (≥1024px) | 2-column (목록 + 상세) |
| Tablet (768~1023px) | 2-column (좁은 목록 + 상세) |
| Mobile (<768px) | 단일 컬럼 (목록 ↔ 상세 전환) |

### 8.2 테마 지원

기존 CSS 커스텀 변수 시스템 활용 — 다크/라이트 모드 자동 대응.

### 8.3 실시간 업데이트

| 이벤트 | 영향 범위 |
|--------|-----------|
| `task_update` | DirectivesList 카드 상태 뱃지, DirectiveDetail 진행 상황 |
| `cli_output` | DirectiveDetail 터미널 출력 (진행 중인 지시) |
| `subtask_update` | DirectiveDetail 서브태스크 목록 |
| `task_report` | DeliverablesList 새 항목 추가 |
| `test_output` | DeliverableTestTab 자동 테스트 출력 스트리밍 |
| `test_complete` | DeliverableTestTab 테스트 결과 카드 업데이트 |
| `preview_ready` | DeliverableTestTab 미리보기 URL 표시 |
| `preview_stopped` | DeliverableTestTab 서버 상태 갱신 |

### 8.4 빈 상태(Empty State) 디자인

| 화면 | 빈 상태 메시지 |
|------|---------------|
| 업무지시 목록 없음 | "아직 업무 지시가 없습니다. 우측의 폼을 사용하여 첫 번째 업무를 지시해보세요." |
| 결과물 목록 없음 | "완료된 결과물이 없습니다. 업무가 완료되면 여기에 결과가 표시됩니다." |
| 상세 미선택 | "좌측 목록에서 항목을 선택하면 상세 내용이 표시됩니다." |
| 테스트 스크립트 없음 | "감지된 테스트 스크립트가 없습니다. 커스텀 명령어를 입력하여 테스트를 실행할 수 있습니다." |
| 체크리스트 없음 | "테스트 체크리스트가 아직 생성되지 않았습니다. 'AI 체크리스트 생성' 버튼을 클릭하세요." |

---

## 9. 기존 기능과의 관계

### 9.1 TaskBoard와의 차이점

| 관점 | TaskBoard (기존) | Directives (신규) |
|------|-----------------|-------------------|
| **목적** | 전체 태스크 관리 (칸반) | CEO 업무지시 중심 |
| **뷰** | 칸반 보드 (컬럼별) | 리스트 + 상세 (마스터-디테일) |
| **대상** | 모든 태스크 (시스템 포함) | CEO가 직접 지시한 태스크 |
| **조작** | 드래그, 다중 작업 | 단일 지시에 집중 |

TaskBoard는 **운영자 관점의 전체 관리 도구**로 유지하고, Directives는 **CEO 관점의 지시/추적 도구**로 분리.

### 9.2 ChatPanel과의 관계

| 기능 | ChatPanel (기존) | Directives (신규) |
|------|-----------------|-------------------|
| `$` 디렉티브 | 채팅 내 인라인 | QuickDirectiveBar로 통합 제공 |
| 에이전트 채팅 | O | X (채팅은 ChatPanel 유지) |
| 진행 추적 | X | O (상태 타임라인, 서브태스크) |

`$` 디렉티브 기능은 ChatPanel에서도 계속 사용 가능. Directives는 더 구조화된 대안.

### 9.3 ReportHistory와의 관계

기존 `ReportHistory` 모달은 Deliverables 뷰의 축약판. Deliverables 뷰 추가 후 ReportHistory는 **AppHeader의 빠른 접근 버튼**으로 유지하되, 클릭 시 Deliverables 뷰로 이동하도록 변경 가능.

---

## 10. 구현 순서

### Phase 1: 인프라 (1단계)
1. `View` 타입 확장
2. Sidebar에 메뉴 추가
3. 로케일 텍스트 추가
4. 빈 뷰 컴포넌트 생성 (스켈레톤)

### Phase 2: 업무지시 센터 (2단계)
1. `DirectivesView` 레이아웃
2. `DirectivesList` + `DirectiveCard` (목록)
3. `DirectiveForm` (지시 생성 폼)
4. `DirectiveDetail` (상세 보기)
5. `DirectiveTimeline` (상태 타임라인)
6. `QuickDirectiveBar` (빠른 지시)
7. WebSocket 실시간 연동

### Phase 3: 결과물 열람 (3단계)
1. `DeliverablesView` 레이아웃
2. `DeliverablesList` + `DeliverableCard` (목록)
3. `DeliverableDetail` (탭 컨테이너)
4. `DeliverableSummaryTab` (요약)
5. `DeliverableCodeTab` (코드 diff)
6. `DeliverableMinutesTab` (회의록)
7. `DeliverableLogTab` (실행 로그)

### Phase 4: 백엔드 확장 (3단계와 병행)
1. `GET /api/tasks/:id/timeline` 엔드포인트
2. `POST /api/tasks/:id/clone` 엔드포인트
3. DB 마이그레이션: `test_runs`, `preview_sessions`, `test_checklists` 테이블 생성
4. 테스트 실행 API: `POST/GET /api/tasks/:id/test`, `GET /api/tasks/:id/test/detect`
5. 미리보기 API: `POST/GET/DELETE /api/tasks/:id/preview`
6. 체크리스트 API: `POST/GET/PATCH/DELETE /api/tasks/:id/checklist`
7. WebSocket 이벤트 추가: `test_output`, `test_complete`, `preview_ready`, `preview_stopped`

### Phase 5: 테스트 센터 (4단계)
1. `DeliverableTestTab` (테스트 탭 메인 컨테이너)
2. `AutoTestSection` (자동 테스트 실행 + 결과 표시)
3. `TestResultCard` (테스트 결과 카드)
4. `PreviewSection` (미리보기 서버 관리)
5. `ChecklistSection` + `ChecklistItem` (체크리스트)
6. `src/api/testing.ts` (API 클라이언트)
7. WebSocket 이벤트 핸들러 연동

### Phase 6: 연결 & 마감 (5단계)
1. ReportHistory → Deliverables 연동
2. AppHeader 빠른 지시 버튼
3. Deliverables 목록 카드에 테스트 상태 뱃지 표시
4. DirectiveDetail에서 "테스트 실행" / "미리보기" 액션 버튼
5. 반응형 레이아웃 최적화
6. 빈 상태 / 로딩 / 에러 처리
7. 빌드 검증 및 통합 테스트

---

## 11. 파일 변경 범위 요약

| 카테고리 | 파일 수 | 주요 파일 |
|----------|---------|----------|
| 신규 생성 (Directives) | 7 | `directives/` 7개 |
| 신규 생성 (Deliverables) | 9 | `deliverables/` 본체 6개 + `testing/` 하위 5개 - 탭 공유 2개 |
| 신규 생성 (Testing 하위) | 5 | `AutoTestSection`, `PreviewSection`, `ChecklistSection`, `TestResultCard`, `ChecklistItem` |
| 신규 API 클라이언트 | 1 | `src/api/testing.ts` |
| 기존 수정 (프론트) | 6 | App.tsx, Sidebar.tsx, AppHeader.tsx, useAppLabels.ts, api/tasks.ts, useAppWebSocket.ts |
| 백엔드 추가 | 3~4 | routes 핸들러, 마이그레이션, 프로세스 매니저 |
| **합계** | **~30** | |

---

## 12. 품질 기준

- [ ] TypeScript 컴파일 에러 0개
- [ ] Vite 빌드 성공
- [ ] 다크/라이트 모드 정상 동작
- [ ] 모바일 반응형 레이아웃 동작
- [ ] 기존 TaskBoard 기능 영향 없음
- [ ] 기존 ChatPanel `$` 디렉티브 기능 유지
- [ ] WebSocket 실시간 업데이트 정상 동작 (기존 + 신규 4개 이벤트)
- [ ] 로케일 4개 언어 텍스트 완비 (ko, en, ja, zh)
- [ ] 자동 테스트 실행 및 결과 파싱 정상 동작
- [ ] 미리보기 서버 시작/중지 정상 동작
- [ ] 체크리스트 AI 자동 생성 및 수동 CRUD 정상 동작
- [ ] 테스트 프로세스 종료 시 좀비 프로세스 없음
- [ ] 미리보기 서버 종료 시 포트 정상 해제
